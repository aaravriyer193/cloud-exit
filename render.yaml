services:
  - type: web
    name: cloud-exit-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: RELAY_TOKEN
        generateValue: true
